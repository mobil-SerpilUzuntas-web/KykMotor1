@model Product 

<div class="col-md-4 mt-lg-3">
<div class="card">
@*  <img src="~/images/indeximg/@Model" alt="@Model"/>*@
   <div class="card-body">
    <h4>
       <small class="text-muted">@Model.Price</small>
    </h4> 
    <small class="text-muted">
         <i class="fas fa-lira-sign"></i>@Model.CategoryId, @Model.Name
    </small>
   </div>
       <div class="card-footer text-center">
         <a href="#" class="btn btn-outline-primary btn-sm">Details</a>
          <a href="#" class="btn btn-outline-primary btn-sm ml-3">Add To Card</a>

        </div>
</div>
</div>